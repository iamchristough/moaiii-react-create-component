// @flow
import * as React from "react";

type Props = {};
type State = {};

const _COMPONENT_NAME_ = ( {props} ): React.Element<'div'> => {
  const {} = props;

  return(
    <div className={`_COMPONENT_NAME_`}>
    
    </div>
  )
}

export default _COMPONENT_NAME_;