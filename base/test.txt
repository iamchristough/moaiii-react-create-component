import reducer from "./*COMPONENT_NAME*.reducer";
import * as actions from "./*COMPONENT_NAME*.action";
import store from '../../../db/store';

describe('*COMPONENT_NAME*', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});